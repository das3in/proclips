%h1 Teams
= link_to "New Team", new_team_path, class: "btn btn-primary"
%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Tag
      %th League
      %th Details
  %tbody
    - @teams.each do |team|
      %tr
        %td= team.name
        %td= team.tag
        %td= team.league.name
        %td= link_to "Details", team_path(team)
